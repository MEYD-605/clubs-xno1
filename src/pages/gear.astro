---
import BaseLayout from '../layouts/BaseLayout.astro';
import Navbar from '../components/navbar';
import Footer from '../components/footer';
import FloatingButtons from '../components/floating-buttons';
import '../styles/globals.css';

const title = "Bo's Favorite Gear | Club S by Bo";
const description = "A curated list of photography gear, creator tools, and lifestyle picks";

const categories = [
  { id: "photography", name: "Photography", icon: "📸" },
  { id: "tech", name: "Tech & Creator Tools", icon: "💻" },
  { id: "bartending", name: "Bartending", icon: "🍸" },
  { id: "lifestyle", name: "Lifestyle", icon: "⭐" },
];

const photographyGear = [
  { name: "Sony A7III (Soul-Brews Edition)", description: "กล้องตัวเก่งที่ผมไว้ใจที่สุด ให้โทนสีผิว (Skin Tone) ที่เป็นเอกลักษณ์สไตล์ Soul-Brews", price: "฿65,000" },
  { name: "Sony 35mm f/1.4 GM", description: "เลนส์ระยะสายตาที่คมที่สุดเท่าที่เคยใช้มา ตัวเดียวจบได้ทุกสถานการณ์", price: "฿49,990" },
  { name: "Godox AD200 Pro", description: "พกพาง่าย แต่พลังแรงพอจะสู้แสงแดดประเทศไทยได้สบาย", price: "฿12,900" },
];

const techGear = [
  { name: "MacBook Pro M3 Max", description: "สำหรับการประมวลผลวิดีโอ 8K และการรัน AI Model เฉพาะกิจ (Gemma 2)", price: "฿129,900" },
  { name: "Proxmox Server Cluster", description: "ระบบหลังบ้านที่รัน n8n และ automation ทั้งหมดที่เราเห็นในเว็บนี้", price: "24/7 Automation" },
  { name: "WD My Passport SSD 2TB", description: "ความเร็วสูงพิเศษสำหรับงานสำรองข้อมูลงานลูกค้า", price: "฿9,900" },
];

const bartendingGear = [
  { name: "Professional Boston Shaker", description: "High-quality stainless steel shaker for cocktail mixing", price: "฿1,800" },
  { name: "Japanese Jigger", description: "Precision measuring tool for perfect cocktail proportions", price: "฿850" },
  { name: "Cocktail Recipe Book", description: "Comprehensive guide to classic and modern cocktails", price: "฿1,200" },
];

export const prerender = true;
---

<BaseLayout>
    <Navbar client:load />
    
    <main class="min-h-screen pt-24">
      <!-- Hero Section -->
      <section class="section-padding gradient-mesh">
        <div class="container mx-auto container-narrow text-center">
          <div class="inline-block px-4 py-2 rounded-full mb-6 font-medium" style="background: rgba(255,107,53,0.1); color: #FF6B35;">
            Curated Recommendations
          </div>
          <h1 class="text-display-lg mb-6">Bo's Favorite Gear</h1>
          <p class="text-body-lg mb-8">
            A handpicked selection of tools and equipment I personally use and recommend
          </p>
          <div class="flex flex-wrap justify-center gap-3">
            {categories.map((category) => (
              <a 
                href={`#${category.id}`}
                class="inline-flex items-center px-4 py-2 rounded-full transition-all hover-lift glass"
              >
                <span class="mr-2">{category.icon}</span>
                <span>{category.name}</span>
              </a>
            ))}
          </div>
        </div>
      </section>

      <!-- Affiliate Disclosure -->
      <section class="py-8" style="background: rgba(255,107,53,0.1);">
        <div class="container mx-auto px-4">
          <div class="max-w-3xl mx-auto text-center">
            <p class="text-body text-sm">
              <strong>Affiliate Disclosure:</strong> Some links on this page are affiliate links. If you make a purchase through these links, I may earn a commission at no extra cost to you. I only recommend products I personally use and believe will add value to your creative journey.
            </p>
          </div>
        </div>
      </section>

      <!-- Photography Gear Section -->
      <section id="photography" class="section-padding">
        <div class="container mx-auto container-wide">
          <div class="flex items-center mb-12">
            <span class="text-3xl mr-4">📸</span>
            <h2 class="text-display-md">Photography Gear</h2>
          </div>

          <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            {photographyGear.map((item) => (
              <div class="card-2026 p-6">
                <h3 class="text-heading mb-3">{item.name}</h3>
                <p class="text-body mb-4">{item.description}</p>
                <div class="flex justify-between items-center">
                  <span class="font-bold text-accent" style="color: #FF6B35;">{item.price}</span>
                  <a href="#" class="text-accent hover:underline" style="color: #FF6B35;">Check it out →</a>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      <!-- Tech Gear Section -->
      <section id="tech" class="section-padding gradient-dark">
        <div class="container mx-auto container-wide">
          <div class="flex items-center mb-12">
            <span class="text-3xl mr-4">💻</span>
            <h2 class="text-display-md">Tech & Creator Tools</h2>
          </div>

          <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            {techGear.map((item) => (
              <div class="glass-card p-6">
                <h3 class="text-heading mb-3">{item.name}</h3>
                <p class="text-body mb-4">{item.description}</p>
                <div class="flex justify-between items-center">
                  <span class="font-bold text-accent" style="color: #FF6B35;">{item.price}</span>
                  <a href="#" class="text-accent hover:underline" style="color: #FF6B35;">Check it out →</a>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      <!-- Bartending Gear Section -->
      <section id="bartending" class="section-padding">
        <div class="container mx-auto container-wide">
          <div class="flex items-center mb-12">
            <span class="text-3xl mr-4">🍸</span>
            <h2 class="text-display-md">Bartending Essentials</h2>
          </div>

          <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            {bartendingGear.map((item) => (
              <div class="card-2026 p-6">
                <h3 class="text-heading mb-3">{item.name}</h3>
                <p class="text-body mb-4">{item.description}</p>
                <div class="flex justify-between items-center">
                  <span class="font-bold text-accent" style="color: #FF6B35;">{item.price}</span>
                  <a href="#" class="text-accent hover:underline" style="color: #FF6B35;">Check it out →</a>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      <!-- Lifestyle Section -->
      <section id="lifestyle" class="section-padding" style="background: rgba(255,107,53,0.05);">
        <div class="container mx-auto container-wide">
          <div class="flex items-center mb-12">
            <span class="text-3xl mr-4">⭐</span>
            <h2 class="text-display-md">Lifestyle Picks</h2>
          </div>

          <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
            <div class="card-2026 p-8">
              <h3 class="text-heading mb-4">Marshall Middleton</h3>
              <p class="text-body mb-6">
                ลำโพงที่พกพาไปได้ทุกที่ในกองถ่าย เพื่อสร้างบรรยากาศและ Vibe ที่ดึงอินเนอร์ของนางแบบออกมาได้ดีที่สุด
              </p>
              <div class="flex justify-between items-center">
                <span class="font-bold text-accent" style="color: #FF6B35;">฿14,990</span>
                <span class="text-white/40 text-sm">Bo's Daily Drive</span>
              </div>
            </div>

            <div class="card-2026 p-8">
              <h3 class="text-heading mb-4">Bellroy Desk Pouch</h3>
              <h3 class="text-heading mb-4">Bellroy Desk Pouch</h3>
              <p class="text-body mb-6">
                กระเป๋าใส่อุปกรณ์จุกจิกที่ช่วยให้โต๊ะทำงาน (และกระเป๋ากล้อง) เป็นระเบียบเรียบร้อย สไตล์มินิมอล
              </p>
              <div class="flex justify-between items-center">
                <span class="font-bold text-accent" style="color: #FF6B35;">฿2,450</span>
                <span class="text-white/40 text-sm">Organization Soul</span>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- CTA Section -->
      <section class="section-padding overflow-hidden relative" style="background: #FF6B35;">
        <div class="absolute inset-0 opacity-10 pointer-events-none font-black text-[10vw] uppercase leading-none select-none">
          Soul Brews Mastery Gear Selection
        </div>
        <div class="container mx-auto container-narrow text-center relative z-10">
          <h2 class="text-display-md mb-6 text-white italic">"The right tools make all the difference."</h2>
          <p class="text-xl mb-8 text-white/90 font-medium">
            มีคำถามเรื่องอุปกรณ์ตัวไหน หรืออยากให้ข้าพเจ้า (Oracle) ช่วยจัดสเปกตามงบประมาณ? เจ้านายติดต่อ Bo ได้โดยตรงเลยครับ
          </p>
          <a href="/contact" class="inline-block bg-white text-[#FF6B35] px-10 py-5 rounded-full font-bold text-lg hover:scale-105 transition-transform shadow-2xl">
            Ask Bo Anything
          </a>
        </div>
      </section>
    </main>

    <Footer client:load />
    <FloatingButtons client:load />
</BaseLayout>
