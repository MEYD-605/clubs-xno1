---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import Navbar from '../../components/navbar';
import Footer from '../../components/footer';
import '../../styles/globals.css';

const allPosts = await getCollection('blog');
const sortedPosts = allPosts.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

const title = "บล็อกช่างภาพ กรุงเทพ | เทคนิคถ่ายรูป Portrait & Tips - ClubsxAI";
const description = "บทความจากช่างภาพ Portrait กรุงเทพ Club S เทคนิคการถ่ายรูป เตรียมตัวก่อนถ่าย Portrait วิธีเลือกช่างภาพด่วนส่งรูปไว และเบื้องหลังการรับถ่ายภาพระดับพรีเมียม";
const keywords = "บทความถ่ายภาพ, เทคนิคถ่ายรูป, การเตรียมตัวถ่าย portrait, ClubsxAI blog, photography tips";
---

<BaseLayout title={title} description={description} keywords={keywords}>
  <Navbar client:load />

  <main class="min-h-screen pt-24 bg-black">
    <section class="section-padding gradient-mesh">
      <div class="container mx-auto container-wide text-center">
        <p class="text-sm uppercase tracking-[0.2em] text-accent mb-4" style="color: #FF6B35;">Knowledge Base</p>
        <h1 class="text-display-md mb-6">บล็อกช่างภาพ กรุงเทพ</h1>
        <p class="text-body-lg max-w-2xl mx-auto">
          รวมบทความและเทคนิคดีๆ สำหรับคนที่หลงใหลในภาพถ่าย และเรื่องราวเบื้องหลังเลนส์
        </p>
      </div>
    </section>

    <section class="section-padding">
      <div class="container mx-auto container-wide">
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
          {sortedPosts.map((post) => (
            <a href={`/blog/${post.slug}`} class="group block h-full">
              <div class="card-2026 h-full flex flex-col overflow-hidden hover:shadow-2xl transition-all duration-500 border border-white/5 hover:border-[#FF6B35]/30">
                <div class="relative h-48 overflow-hidden">
                  <div class="absolute inset-0 bg-gradient-to-t from-black/80 to-transparent z-10"></div>
                  {post.data.image ? (
                    <img loading="lazy" 
                      src={post.data.image} 
                      alt={post.data.title}
                      class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-700" 
                    />
                  ) : (
                    <div class="w-full h-full bg-gray-800 flex items-center justify-center text-4xl group-hover:scale-110 transition-transform duration-700">
                      📸
                    </div>
                  )}
                  <div class="absolute bottom-4 left-4 z-20">
                     <span class="text-xs font-medium px-2 py-1 rounded bg-[#FF6B35] text-white">
                       {post.data.tags[0] || 'Article'}
                     </span>
                  </div>
                </div>
                
                <div class="p-6 flex flex-col flex-grow">
                  <div class="flex items-center gap-2 text-xs text-white/40 mb-3">
                    <span>{post.data.pubDate.toLocaleDateString('th-TH')}</span>
                    <span>•</span>
                    <span>{post.data.author}</span>
                  </div>
                  
                  <h2 class="text-xl font-bold text-white mb-3 group-hover:text-[#FF6B35] transition-colors leading-tight">
                    {post.data.title}
                  </h2>
                  
                  <p class="text-white/60 text-sm line-clamp-3 mb-4 flex-grow">
                    {post.data.description}
                  </p>
                  
                  <div class="mt-auto pt-4 border-t border-white/5 flex items-center text-[#FF6B35] text-sm font-medium">
                    Read Article <span class="ml-2 group-hover:translate-x-1 transition-transform">→</span>
                  </div>
                </div>
              </div>
            </a>
          ))}
        </div>
      </div>
    </section>
  </main>

  <Footer client:load />
</BaseLayout>
